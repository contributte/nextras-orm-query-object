<?xml version="1.0"?>
<ruleset name="Contributte">
    <!-- Extending ruleset -->
    <rule ref="vendor/ninjify/coding-standard/contributte.xml">
        <exclude name="SlevomatCodingStandard.ControlStructures.EarlyExit"/>
    </rule>

    <!-- My rules -->
    <rule ref="SlevomatCodingStandard.Files.TypeNameMatchesFileName">
        <properties>
            <property name="rootNamespaces" type="array" value="
                src=>Contributte\Nextras\Orm\QueryObject,
                tests/cases/integration=>Tests\Cases\Integration,
                tests/cases/unit=>Tests\Cases\Unit,
                tests/fixtures=>Tests\Fixtures,
                tests/mocks=>Tests\Mocks,
            "/>
        </properties>
    </rule>

    <rule ref="SlevomatCodingStandard.Files.TypeNameMatchesFileName.NoMatchBetweenTypeNameAndFileName">
        <exclude-pattern>/tests/cases</exclude-pattern>
    </rule>

    <!-- Exclude folders -->
    <exclude-pattern>/tests/tmp</exclude-pattern>
</ruleset>
